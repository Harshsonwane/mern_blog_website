front : npm run dev
backend : node start || nodemon server
we have used flaticon for icons 
read about Link in react 
outlet used for react navigation routes for nested route
read about props,usestate, 
for animation we use frammermotion -> framer-motion


//connecting page to user with useref  react hook  
//read ref in react hook used for refenece to take use to html tag 
toster and toast used for popups 


some command in node -> terminal 
require('crypto').randomBytes(64).toString('hex'); to get hex code
